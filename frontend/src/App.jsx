import{useEffect,useState}from'react';import{Route,Routes}from'react-router-dom';import Navbar from'./components/Navbar';import ProtectedRoute from'./components/ProtectedRoute';import Home from'./pages/Home';import Login from'./pages/Login';import Signup from'./pages/Signup';import Profile from'./pages/Profile';export default function App(){const[toast,setToast]=useState('');const notify=m=>setToast(m);useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),4000);return()=>clearTimeout(t)},[toast]);return <><Navbar notify={notify}/>{toast&&<div className="toast">✓ {toast}<button onClick={()=>setToast('')}>×</button></div>}<Routes><Route path="/" element={<Home/>}/><Route path="/login" element={<Login notify={notify}/>}/><Route path="/signup" element={<Signup notify={notify}/>}/><Route path="/profile" element={<ProtectedRoute><Profile notify={notify}/></ProtectedRoute>}/><Route path="*" element={<Home/>}/></Routes></>}
